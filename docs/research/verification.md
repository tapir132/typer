# Verification record - September 6, 2026

- Baseline: 14 tests passed with `swift test -Xswiftc -warnings-as-errors`.
- Expanded suite: 36 tests passed with the same command (last run 0.531 s).
- Final training readout change: `swift build -Xswiftc -warnings-as-errors` passed.
- Native bundle: `./scripts/build-app.sh` passed; `.build/Typer.app` rebuilt after final changes.
- Signature structure: `codesign --verify --deep --strict .build/Typer.app` passed. This is a local build, not a notarization/distribution claim.
- Whitespace: `git diff --check` passed.
- Research artifact: `output/pdf/typer-realism-research.pdf`, six pages, 26 hyperlink annotations. All six rendered pages visually inspected for clipping, overlap, citation formatting and page breaks.
- Regression coverage includes 500 original source/seed combinations, physical timeline ordering, cancel/failure cleanup, actual monotonic scheduler execution with an injected sink, old-schema decoding, empirical shrinkage, invalid inputs, context isolation, held-out separation, deterministic distances, autocorrelation and profile deletion.
- Controlled generated fixtures improve hold/rollover fit when supplied with matching personal evidence. These fixtures are not human observations.
- At the end of the research pass, no new human sessions, globally posted playback test, external editor receiver measurements, or native UI screenshot/interaction audit had been performed. Those limitations are stated in the research artifact and validation protocol. The subsequent UI work is recorded below.
- The pre-existing untracked `handoff.md` was read and preserved.

## UI and updater follow-up

- Reproduced the reported header movement in a native `NSWindow`/`NSHostingView`: Compose's header began at y = −7 points at 1240 × 800, versus y = 28 for Train and Profiles. At 920 × 660, Compose began at y = −77. The initial regression test failed on clipping and unequal header positions.
- Pinned the header to the window's available geometry and put overflowing workspace content in a scroll view. The same test now passes across Compose, Train, Profiles, Guide, and back to Compose, at both sizes.
- Native screenshots visually checked for all four workspaces at both sizes, the System setup switch alignment, and all eight Guide topics. Artifacts are in `output/layout-after/`; initial reproductions are in `output/layout-before/`. These are offscreen native renders, not screenshots of the still-running older app.
- Added small help buttons using native hover tooltips and click/keyboard popovers beside controls and measurements. Hover timing and live interaction in the user's running app were not exercised; that process still contains the previous build.
- Expanded suite: 41 tests passed with `swift test -Xswiftc -warnings-as-errors`, including layout, local build identity, Sparkle's build-number ordering, no-update reasons, incompatible macOS reporting, and update failure recovery. Additional native screenshot capture passed after extending the QA harness.
- Verified both published appcasts returned HTTP 200. Their latest internal build was `1788545750` from September 4. The inspected local build was newer (`1788722395`), while the running process had started September 4 at 11:20:18 local time. No release containing today's local edits had been published.
- System setup now shows build origin/date, unpublished-change metadata, and update-check results. The existing local timestamp version ordering and signature verification remain in place.
- Rebuilt `.build/Typer.app` after the guide/help changes; signature verification and shell syntax checks passed. Save unfinished work and quit/reopen the rebuilt app to load it. No forced restart, release publication, live recording, or external-editor key injection was performed.
- Older-profile follow-up: added a conditional “Older profile · still usable” explanation in Profiles, a shorter note in Compose's My rhythm controls, and guidance on retaining older data in the Guide. Native layout checks passed with an older profile loaded from isolated test preferences at both supported test sizes; Profiles and Compose screenshots were visually inspected in `output/older-profile-qa/`. The built-in baseline does not receive the older-profile label.
- Legacy locking follow-up: replaced that explanatory-only treatment with separate, playback-only Legacy profiles. New recordings cannot update them, old-format recordings are rejected as new training, and the twelve-current-sample limit leaves the Legacy archive intact. Launch migration preserves existing Legacy IDs/statistics or recovers a separate Legacy rhythm from older samples if an earlier build replaced it. Deletion tests verify that removing one profile preserves the other and does not recreate deleted data on launch.
- The saved-total / profile-training discrepancy was verified using counts only from local preferences: four Legacy samples and one new Copy sample, with the current profile trained on one. Both views now explicitly distinguish those counts. Native screenshots of the two separate profiles and the training destination were inspected in `output/legacy-lock-qa/`. Full suite: 46 tests passed with warnings treated as errors. The user's live preferences were not modified during this work; migration takes effect when the rebuilt app is relaunched.

## Settings guide cleanup — September 8, 2026

- Moved the guide into Settings, removed the main Guide tab and workspace guide links, and kept hover/click question-mark help. The gear and ⌘, open General; Help → Typer Guide opens Guide in the same sheet.
- Shortened the eight guide topics, tooltips, and permission/update copy. Detailed control, training, and validation notes are collapsed until requested. Permission actions now sit beside their corresponding status.
- Audited Live capture's existing idle behavior: sessions stop at 15 minutes, WPM includes elapsed idle time, motor pairs exclude intervals over 2.5 seconds, and pause summaries can retain gaps up to 60 seconds. Added a concise limitation beside Live capture and in the guide. This change does not alter capture, timing, or stored profiles.
- `TYPER_LAYOUT_SNAPSHOTS=output/settings-guide swift test -Xswiftc -warnings-as-errors`: all 78 tests passed. Native window regression verifies that opening the Settings guide, changing its topic/tab, and dismissing it keeps the same training editor and its unfinished text. Header-position checks still pass at 1240×800 and 920×660.
- Visually checked offscreen native renders of the three workspaces, Settings → General, all eight Guide topics at the actual sheet size, and the Live capture limitation. Screenshots are under `output/settings-guide/`. No live input was recorded for these checks.

## Background Live capture — September 8, 2026

- Extended explicitly started Live sessions to one hour. ContinuousClock includes idle and sleep time; timer ticks, incoming events, and wake check expiry. App activation, clicks, scrolling, Secure Input, sleep/session deactivation, and gaps over 2.5 seconds break sequences.
- Active WPM counts contiguous character arrivals and typing/deletion intervals, excluding the first key after a break. Navigation, repeated keys, invalid timestamps, and late releases cannot extend those intervals. Saving now requires 35 characters, 20 usable pairs, and finite active pace.
- Live evidence excludes thinking-pause summaries during capture, retraining, playback, and validation. Existing Live motor evidence remains usable; older WPM is estimated from retained intervals when retraining or validating. In-app pause learning remains available. Added only optional aggregate activity counters to persisted samples.
- Removed full-history summarization from the event callback. Preview work is limited to the latest 2,000 records, at most every two seconds; Stop summarizes the whole session once. Raw session records are capped at 100,000 and saved distributions stay bounded.
- `TYPER_LAYOUT_SNAPSHOTS=output/background-capture swift test -Xswiftc -warnings-as-errors`: all 90 tests passed. Twelve new regressions cover an hour of scattered typing, explicit and timed boundaries, missing releases, edits/repeats, sparse isolated keys, preserved rollover, deadline arithmetic, malformed times, record/preview bounds, old/new persistence, old Live pause suppression, and unchanged Freewrite pause learning.
- The simulated hour exactly matched the same active bouts' speed, timing evidence, and digraphs. The 100,000-record stress fixture passed with bounded previews and persisted summaries. These are synthetic fixtures, not a claim that a human hour was recorded or every foreground app was validated.
- Visually checked the native Live capture screen and Settings training guide in `output/background-capture/`. No user input was captured and no saved user profiles were changed during testing.

## Rebindable controls — September 13, 2026

- Added Settings → Shortcuts for Arm, Pause/resume, Skip wait, and Stop using the local-monitor recorder approach from Cadence. Bindings persist separately from typing presets. Duplicate/reserved/OS conflicts keep the previous value; release-before-save, Escape cancellation, focus/view cleanup, Caps Lock normalization, modifier-only chords and Carbon repeat suppression have regression coverage.
- Routed saved bindings into the menus, Compose, guide, preview, and fullscreen overlay. Stop registration failures are surfaced; arming requires an available primary or backup stop and available Pause/Skip registrations. Control–Esc remains a reserved backup.
- Full suite: 101 tests pass with warnings treated as errors. Native Settings integration records a replacement and checks its visible title, then switches tabs during recording and verifies registration is restored. Offscreen layouts are in `output/shortcuts-qa/`.
- `scripts/verify-shortcuts.sh` passed an automated global-delivery check while its application was inactive: Control–Option–Command with F17/F18/F19, followed by a recorder change to Control–Option–Command–D and successful delivery of the new key. Preferences reload retained the change. The test uses isolated preferences and does not load user profiles. Injected F20 did not produce a callback in this environment; physical F20 and physical modifier-only chords were not tested. The check does not claim every hardware/layout combination was exercised.
- The standalone playback checker initially failed because its compile list omitted the new CaptureActivity dependency. Fixed that list; all three native playback scenarios pass again. CI now compiles both native verification tools so source-list drift is caught without a GUI session.
- Updated versioned-release creation to target the explicitly verified commit, and described the release confirmation as native UI/playback smoke checks. No Apple Developer signing/notarization capability is implied by these changes.

## Safari event comparison — September 13, 2026

- Added a loopback-only page, bounded explicit keyboard recorder, native fixed-fixture launcher, command-line comparison, and JSON reports. Browser automation is labelled separately and cannot satisfy the physical-reference requirement. No Safari automation preferences, TCC grants, keyboard remaps, installed Typer bundle or saved profiles were changed.
- Safari 26.5.2 on macOS 15.7.7, U.S. layout: the original six HID scenarios produced the expected text. The Unicode trace exposed fallback characters using `KeyA`, including an em dash whose key-up reported `a`.
- Added fourteen direct U.S. Option-symbol mappings. Tests translate each through Apple's installed U.S. keyboard layout and require the intended character with no dead-key state. A second regression checks modifier barriers and cancellation at every action.
- The extended symbol-only HID check lost ordinary keys while modifier events arrived; other runs interrupted on focus identity. The same symbol fixture delivered correctly when addressed directly to Safari's process. The exact intercepting component was not identified. Production now uses process-addressed events for the selected app, including cleanup releases; app-switch pausing remains.
- All seven native Safari scenarios pass with process-addressed delivery: plain text, capitals/symbols, overlap, corrections, line breaks, Unicode and fourteen Option symbols. Final text matches, key pairs are balanced, and recorded events are trusted in these controlled runs. Artifacts: `output/browser-check/native-suite-process/`. A trusted event is not a physical-input certificate.
- Full Swift suite: 103 tests pass with warnings treated as errors. The 16 JavaScript comparison tests pass, including malformed data, missing keys, provenance rejection, changed key values, paste/drop, composition, idle boundaries, insufficient samples and unequal-size Wasserstein distance. The native Safari helper compiles with warnings treated as errors. Native AppKit playback (three scenarios) and shortcut delivery/recording checks also pass.
- Playwright verified the local page's automated provenance label, text capture and blur interruption. Desktop and 390-pixel-wide renders were visually inspected, with no horizontal overflow at the narrow size. A real Safari window screenshot was also inspected. UI artifacts are under `output/playwright/`.
- A physical reference was requested, but no exported `typer-safari-*.json` sample was available at verification time. The physical-to-Typer comparison remains pending. There is no human indistinguishability claim. Accents requiring dead keys and emoji retain Unicode fallback; their key/code and composition sequences can differ from physical input.
